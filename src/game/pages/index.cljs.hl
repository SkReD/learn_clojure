(page "index.html"
      (:require [game.logic])
      (:require [debug.repl]))

(def currentMarkType (cell :crosses))
(def board (cell (@game.logic/board)))

(defn set-cell-mark
  [row-index cell-index]
  )

(html
  (body
    (div :class "game-table"
      (loop-tpl :bindings [row board]
        (div :class "game-row"
             (loop-tpl :bindings [row-cell row]
                       (span :class "game-cell" :click #(js/console.log 2) row-cell)))))))

