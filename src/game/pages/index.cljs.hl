(page "index.html"
      (:require [game.logic] [debug.repl]))

(def currentMarkType (cell :crosses))

(html
  (body
    (div :class "game-table"
      (loop-tpl :bindings [row game.logic/board]
        (div :class "game-row"
             (loop-tpl :bindings [row-cell row]
                       (span :class "game-cell" row-cell)))))))
